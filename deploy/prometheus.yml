global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'hr-backend'
    metrics_path: /metrics
    static_configs:
      - targets: ['backend:8034']
        labels:
          service: hr-backend
